# RC1 Pre-Live Failure Report

## Verdict

**NOT READY** — STOP deployment. Do not merge to main for live trading.
Do not recommend production deployment.

Generated by `scripts/run_final_pre_live_rc1.py` (timestamp filled at run time).

## Failed / Incomplete Checklist Items

1. **Merge candidate PRs after code review** — blocked.
   Open draft PRs still have failing Lint & Format and/or Type Check.
   Integration Tests are skipped in CI. No PR was merged by this run.
2. **Unit / integration / regression tests** — must be green before any merge.
   See PR CI and local test logs attached with this evidence pack.
3. **Staging DB migrations** — NOT applied (explicitly forbidden).
   QuantForg Supabase project `otqyhlmwaifokrczryrc` permission denied.
   Unrelated org project Jimvio was not touched.
4. **Pending migrations** — unverified against QuantForg remote.
   Repo has `supabase/migrations/*.sql` (49 up) and alembic baseline.
5. **Deploy to staging** — not performed (Vercel MCP unauthenticated).
   Production Railway process health is not a staging gate pass.
6. **Live evidence** — process `/health` + postgres healthy.
   Gateway / OMS / MT5 / AI remain UNKNOWN (401 on ops endpoints).
   RC1 validation route 404 on production (PR #57 not merged).
7. **RC1 paper** — executed locally with live health attached.
8. **RC1 shadow** — executed locally with live health attached.
9. **Acceptance gates** — NOT all PASS (infra UNKNOWN).
   Release PR for production go-live is not opened.

## Remediation Steps

1. Fix Lint & Format on merge candidates (#57, #56, stacked ITE PRs).
   Re-run CI until Lint, Type Check, and Unit Tests pass.
2. Un-skip / run Integration Tests in CI against staging credentials.
3. Authenticate an operator and capture:
   `GET /ite/ops/services-health` plus MT5 / OMS / gateway facts
   into `docs/production/pre_live_evidence/`.
4. Deploy RC1 validation endpoints to a staging Railway/Vercel env
   (not unrestricted live trading).
5. Set `PRODUCTION_VALIDATION_MODE=true` and run
   `VALIDATION_EXECUTION_MODE=paper` then `shadow` on staging
   for at least one full session cycle.
6. In staging only, apply pending Supabase migrations after review;
   confirm `list_migrations` shows no unexpected pending.
7. Re-run acceptance gates; only then open a release PR recommending
   limited live pilot.

## Hard Rules Affirmed

- No automatic merge
- No push to main
- No automatic database migrations
- No bypass of acceptance gates
- No fabricated Gateway / OMS / MT5 / AI health

## Artifacts

- `docs/production/RC1_VALIDATION_REPORT.md`
- `docs/production/pre_live_evidence/live_health_probes.json`
- `docs/production/pre_live_evidence/rc1_paper_result.json`
- `docs/production/pre_live_evidence/rc1_shadow_result.json`


---

Run stamp: `2026-07-31T12:20:10.294607Z`
Combined recommendation: `NOT READY`
Primary report: `/workspace/docs/production/RC1_VALIDATION_REPORT.md`
