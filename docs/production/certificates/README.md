# Production Acceptance Certificates

`Production_Acceptance_Certificate.md` is generated automatically by the
Execution Evidence Collector when a complete successful real production
execution is observed (OMS + Gateway + MT5 + Broker PASS, broker ticket > 0).

Until then, no certificate file is issued — status remains **NOT VERIFIED**.
