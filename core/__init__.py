"""QuantForg core package — cross-cutting infrastructure primitives.

This package holds shared concerns that every layer may depend on:
configuration, logging, security primitives, database session factories,
and the dependency-injection container.

Business logic never lives here.
"""

__version__ = "1.0.0"
