"use client";

/** Shared realtime hooks — re-exported for convenient imports. */
export {
  useRealtime,
  usePortfolioStream,
  useOrdersStream,
  usePositionsStream,
  useNotificationsStream,
  useMarketStream,
  useActivityStream,
  useBrokerStatusStream,
  useDashboardStream,
  useExecutionStream,
} from "@/hooks/realtime";
