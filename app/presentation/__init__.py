"""Presentation layer — HTTP interface.

FastAPI routers, middleware, dependency providers, and response schemas.
This layer translates HTTP into application-service calls and maps domain
exceptions to HTTP error responses. It contains no business logic.
"""
